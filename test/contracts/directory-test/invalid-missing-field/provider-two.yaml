consumer:
  provider_two:
    product_api:
      OK:
        response:
          statusCode: 200
          body:
            - id: "Joi.number().integer()"
              name: "Joi.string()"
              description: "Joi.string()"
